package com.swmansion.enriched.markdown.utils.common

/** BuildConfig-only shim. This suite exercises links, not optional math rendering. */
object FeatureFlags {
  const val IS_MATH_ENABLED = false
}
