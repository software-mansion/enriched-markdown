package com.swmansion.enriched.markdown

import org.junit.Assert.assertTrue
import org.junit.Test

// Harness placeholder that keeps the RN Android unit test task from failing under
// Gradle 9's failOnNoDiscoveredTests when no real suites are present yet. The
// library's Kotlin unit tests live alongside this file under android/src/test and
// run via `:react-native-enriched-markdown:testDebugUnitTest`.
class HarnessSmokeTest {
  @Test
  fun harnessRuns() {
    assertTrue("Android unit test task is wired and running", true)
  }
}
