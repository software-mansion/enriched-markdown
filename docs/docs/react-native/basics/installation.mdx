---
sidebar_label: Installation
sidebar_position: 1
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Installation

This section walks you through adding `react-native-enriched-markdown` to your app - checking the prerequisites, installing the package, and rebuilding the native projects.

## Prerequisites

The library works only with the [React Native New Architecture (Fabric)](https://reactnative.dev/architecture/landing-page). It's enabled by default on recent React Native versions; if your app still runs the old architecture you'd need to switch before installing.

It also needs these minimum platform versions:

- **Android** - `minSdk 24` (Android 7.0).
- **iOS** - React Native's own minimum deployment target; a few features additionally require iOS 16+ (for example [`contextMenuItems`](/react-native/api-reference/enriched-markdown-text#contextmenuitems)). The standalone iOS SDK requires iOS 16+.
- **macOS** (via [react-native-macos](https://github.com/microsoft/react-native-macos)) - 14.0.

For the exact React Native versions we support, see [Compatibility](/misc/compatibility).

## Install the package

Install the package with your package manager of choice:

:::caution
todo verify pnpm does not break install
:::


<Tabs groupId="package-managers">
  <TabItem value="npm" label="npm">

```bash
npm install react-native-enriched-markdown
```

  </TabItem>
  <TabItem value="yarn" label="yarn">

```bash
yarn add react-native-enriched-markdown
```

  </TabItem>
  <TabItem value="pnpm" label="pnpm">

```bash
pnpm add react-native-enriched-markdown
```

  </TabItem>
</Tabs>

The library contains native code, so rebuild your app after installing. On iOS install the pods first:
```bash
cd ios && pod install
```

## Expo

Install with the Expo CLI so the correct version is picked for your SDK:

```bash
npx expo install react-native-enriched-markdown
```

Then regenerate the native projects:

```bash
npx expo prebuild
```

:::note
The library needs native code, so it won't run in Expo Go. Use a [development build](https://docs.expo.dev/develop/development-builds/introduction/) instead.
:::

## Nightly builds

To try features before they land in a stable release, install the nightly build:

<Tabs groupId="package-managers">
  <TabItem value="npm" label="npm">

```bash
npm install react-native-enriched-markdown@nightly
```

  </TabItem>
  <TabItem value="yarn" label="yarn">

```bash
yarn add react-native-enriched-markdown@nightly
```

  </TabItem>
  <TabItem value="pnpm" label="pnpm">

```bash
pnpm add react-native-enriched-markdown@nightly
```

  </TabItem>
</Tabs>

:::caution
Nightlies are published to npm automatically and may contain breaking changes.
:::

<hr />

You're set up. Next let's try [building your first view and editor](/react-native/basics/your-first-project).
