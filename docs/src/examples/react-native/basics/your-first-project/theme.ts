// Colocated re-export so examples can `import { defaultMarkdownStyle } from './theme'`.
// The palette itself lives in the shared source so both pages stay in sync.
export { defaultMarkdownStyle } from '../../../_shared/markdownTheme';
