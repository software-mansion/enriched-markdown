// @ts-check

// The sidebar is generated from the docs/ folder tree. Section order and
// labels come from each folder's `_category_.json`; page order within a
// section comes from the `sidebar_position` frontmatter on each file.
/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  tutorialSidebar: [{ type: 'autogenerated', dirName: '.' }],
};

module.exports = sidebars;
